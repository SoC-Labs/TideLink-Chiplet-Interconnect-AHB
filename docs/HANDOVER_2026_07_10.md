# Handover — 2026-07-10 (session c508e1e0)

Written for the other TideLink sessions. **Four items below contradict memory files you wrote.**
I have not edited your memory entries; the evidence is here so you can check it and correct them yourself.

T-3wk to tapeout. Read §1 first — it is one afternoon of work and it is why most of the rest happened.

**UPDATE (evening): wired the guards in + fixed gate integrity + built the correct instruments.**
- `sim_gate` + `merge_guard` + the new `zeropoke_por` suite are now CI jobs (§1). Gate is 11 suites.
- **Gate-integrity bug fixed:** the `sim_gate` clean-list was an enumerated subset that rotted, so a
  suite reused a stale `simv` ("../simv up to date", t32 PASS in 2s) and silently tested OLD RTL. Now a
  `rm -rf sim_build*` glob — cannot rot. (`dfd484a`) This is the same false-green class as §1; re-run any
  "green" you got from the old clean-list.
- **`0x1AC`/`0x1B0`/`0x1B4` HARD-STALL the PS thread** (board-proven: uninterruptible AXI hang, external
  SIGKILL only — NOT a bus error, correcting the old memory note). `lane_health_preflight.sh` Phase 2 and
  `reg_safety_probe.sh` are hard-disabled/superseded. The correct eye instrument is **`eye_margin.sh`**:
  read the calibrator's own per-lane eye-width `0x2150` (`best_run` 0..16, `lane_passed[13]`, `0xE7`
  marker) via `0x2154` after `rcp()` bring-up — all safe Region-10 registers, no tap sweep.
- **Deploy gotcha:** `deploy_pair` defaults `TD_DEPLOY_DIR=/tmp/tidelink_deploy_l7`. For the iter5/6
  bitstream you MUST `export TD_DEPLOY_DIR=/tmp/td_iter5` or you flash a stale bitstream (marker reads
  `0x00`, and its degenerate build is what made `0x1AC` hang the first time).

---

## 1. `make sim_gate` is wired into NO CI hook. This is the root cause of everything else.

Verified: zero references to `sim_gate` in `.gitlab-ci.yml`, `ci/`, `scripts/`, `.github/`.
The `v1_elab` suite that would have caught `a405809` **already existed** on the trunk (`5c4665e`),
wired into `sim_gate` and `sim_gate_quick`. Nothing ever ran it.

**Fix this before anything else on this page.** Four of the five tapeout defects below survived to
three weeks before tapeout because a green gate existed and no hook invoked it.

---

## 2. Branches (none of this is visible from memory alone)

| branch | tip | what |
|---|---|---|
| `wip/iter6-integ` | `35782e9` | our integration line. Pulls `tl39.py`+`tlfifo.py`, `5e46be3`, `c8da4fc`, `5fdf6de`. Adds `merge_guard.sh`, harness hardening. |
| `wip/apb-arb-onto-integ` | `cf545f3` | `8eff9ce` (fc_cfg APB arbiter) cherry-picked **cleanly** onto `integ`, test-first. |
| `fix/integ-v1-elab` | `cba737b` | reverts `a405809`; adds an `asic_v1_elab` gate. **Fixes the chip's default build path.** |
| `wip/zeropoke-por-claude` | `bb96475` | true zero-poke: one wrapper parameter. |

`silicon/iter5-deployed` = `0f54141` tags the exact RTL of the bitstream that produced today's soak.

---

## 3. CORRECTION — `a405809` breaks the chip build. Please let us revert it.

It is your commit, so this is a request, not a merge.

`a405809` threaded `obs_*` ports through 7 files in `src/rtl/local_overrides/` and **changed zero
flists** (its own message: *"No apb_regs / flist / fpga change"*). The instantiator
(`local_overrides/WlinkGenericFCReplayV2_13.v:185`) drives ports that the **submodule** definition of
`WlinkGenericFCReplayAddrSync_18` does not declare. It is a def/instantiation **split**, not two defs
resolved by flist order.

| flist | AddrSync_18 | result |
|---|---|---|
| `tidelink_fpga.flist` (V1 cocotb) | SUB `:186` | **BROKEN** |
| `tidelink_fpga_v2.flist` (V2 cocotb) | LOC `:230` | OK ← *the only one the V2-only gate runs* |
| `tidelink_top_full_asic.flist` (**chip default**) | SUB `:126` | **BROKEN** |
| `tidelink_top_full_asic_v2.flist` | SUB `:184` | **BROKEN** |

Measured on pristine `integ/tidelink-soc` @`3f3de09`: V1 ASIC `rc=255`, 7 × `Error-[UPIMI-E]`.
After `git revert a405809`: `rc=0`. The revert applies cleanly; `fe_tx_credit_max` verified intact.

**`0x4403_21BC` has zero readers** anywhere in `pynq_host/`, `cocotb/`, `fpga/`. The obs was built for
the a2l CDC-tear theory that `1c1173e`/`4e7a458` later refuted. Reverting it also dissolves the
`0x21BC` collision with our FCH_OBS and an 896-line merge conflict.

---

## 4. NEW — fifth chip-killer: the V2 ASIC flist does not contain the V2 deskew

```
tidelink_fpga_v2.flist            tidelink_lane_deskew_v2   present
tidelink_top_full_asic_v2.flist   tidelink_lane_deskew_v2   ABSENT   -> pulls deps V1-era deskew
```
`tidelink_phy_sync_detect.sv` is likewise missing from the ASIC V2 flist (instantiated at
`WavD2DGpio_v2.v:863`), which also breaks `$unit`-scope `tidelink_sync_word.svh` visibility.

**So flipping the ASIC default to V2 is necessary but NOT sufficient — the chip would still build a
deskew corrector nobody has ever run on silicon.** `tidelink_lane_deskew_v2.sv` is the module holding
`sync_seen_vec_o` (:1016), `all_sync_seen` (:1350) and the SYNC-reanchor latch (:1489) — everything
this week's autonomy campaign measured. `a405809`-independent. **Needs a PHY owner.**

Also: `syn/asic/fusion-compiler/Makefile:16` uses `override FLIST := .../$(MODULE)_asic.flist`, so the
ASIC flow defaults to V1 **and `make FLIST=…` cannot escape it.**

---

## 5. CORRECTION — chip-killer #3 ("TX eye unconstrained") is OVERSTATED

The evidence in `project_tapeout_three_chip_killers_2026_07_10.md` cites
`imp/ASIC/tidelink_top_full/tidelink_top.sdc` — a **generated, untracked artifact**, mtime May 20-22,
i.e. from *before* the 2026-05-28 constraints rewrite.

The **committed** `syn/asic/fusion-compiler/inputs/constraints.sdc` does constrain the TX eye:
`:102` `create_generated_clock -name pad_clk_tx_fwd`, `:205-206` `set_output_delay … {pad_tx[*]}`.
Per-lane skew budget also exists (`set_max_delay -datapath_only :169`, `set_data_check :183-184`).

What **is** real: `:72 set T_UI_NS 4.0` (250 MHz) against a 100 MHz target — which **over**-constrains,
not under — and **no runtime trim in the ASIC** (`g_passthru`; IDELAYE2 is Xilinx-only), so an
off-centre eye has no recovery knob. Budgets are admitted placeholders (`:66-71`), never characterised
against the TSMC65 I/O cell + package + channel. `div-2` is a genuine margin lever, but you must also
set `T_UI_NS=20.0` or STA still times at 4 ns.

---

## 6. CORRECTION — do NOT build `feat/epoch-anchor-ab`

I nearly recommended it, then the RTL refuted me.

Today's soak settled the autonomy blocker: **`FAILOPEN ⇔ sync_seen_vec != 0xE4`, 6/6, both dies.**
That is an *identity*, not a correlation: `sync_seen_vec_o` (`tidelink_lane_deskew_v2.sv:1016`) **is**
the net that `all_sync_seen` (`:1350`) reduces, and `reanchored` latches iff `all_sync_seen` (`:1489`).
So the "anchor re-latch lottery" is a **SYNC-coverage lottery** — if a lane never matches, *no retry
count can win it*. FIX-3/FIX-4 and the retry-jitter LFSR were built on the wrong model.

EPOCH does **not** remove that requirement: it gates on `ep_all_fresh = &(ep_fresh | ~lane_mask)`
(`:1186`), the structurally identical cross-lane AND, and swaps a *continuous* SYNC beacon for a
*one-shot* training-exit edge — **fewer** capture chances for a marginal lane. Our own tree records the
silicon result at `WavD2DGpio_v2.v:758-763`: *"die_a's dist-4/5 lanes LOCK but never satisfy ep_match →
epoch_anchored=0 forever (silicon-confirmed)."* `b432ade` is also an older divergent line with **no
anchor-verify FSM at all**.

Related correction: **`anchor_timeout` does not mean "the anchor never committed."** The release gate
needs `ws_anchor_q && ws_verify_q` (`axi_chiplet_controller.sv:4845`); `anchor_timeout` = that combined
gate never released. `LATE` cycles have `anchor_timeout=1` **with** a committed anchor. There is **no
role asymmetry** anywhere in the anchor/verify RTL.

**The next step is a measurement, not a fix.** During a FAILOPEN, read the missing lane's Hamming
distance: `sync_dist_vec` `0x4403_21AC`, lane-selected via `0x21B0`.
`> 5` ⇒ hard eye failure, only physical levers (per-lane IDELAY, div-2, reduced-lane link).
`≤ 5` with the sticky bit unset ⇒ the confirm-run FSM is rejecting it, and a one-line lever exists.
⚠ The winscan FSM **owns** `0x21B0`/`0x21B4` and `0x21AC` has bus-errored. **Disarm autonomy first.**

---

## 7. Zero-poke is delivered (sim-proven). It was one parameter.

`fpga/vivado_ip/tidelink_vivado_wrapper.v` forwards a *curated* parameter list to `tidelink_top`
(`USE_IDELAY`, `USE_CLKBUF`, …) and **never listed `NEGO_CFG_RESET`**. So every packaged IP synthesised
the RTL default `7'h00` ⇒ `nego_en=0` at POR ⇒ `autonomy_armed` could never assert. **Every "zero-poke"
run this project has ever done began by poking `NEGO_CFG=0x61` over APB.**

Fix (`bb96475`): add `NEGO_CFG_RESET = 7'h61` + `NEGO_TRAIN_CFG_RESET = 16'h0001` to the wrapper.
`tidelink_top`'s default stays `7'h00`, so cocotb/UVM/ASIC consumers are untouched.

Fail-first: `cocotb/tidelink_top_pair/test_zeropoke_por.py` installs a passive monitor that fails the
instant *any* ctrl-reg write hits NEGO_CFG or NEGO_TRAIN_CFG.
* `7'h00` → **FAIL** at the `nego_en` precondition.
* `7'h61` → **PASS**: bilateral `cal=S_DONE`, `fcsm=4`, **zero writes to either register**.
Regressions: `t31`, `t30`, `v2_pair_data`, `v1_elab` PASS. The other six suites were **not run**.

**A parameter survives packaging where a `+define+` does not** — confirmed: `USE_IDELAY`/`USE_CLKBUF`
appear as `MODELPARAM_VALUE` in `imp/fpga/tidelink_ip/component.xml`, and `build_design.tcl` sets zero
`CONFIG.*` overrides. Verify on the next build, structurally, not by md5:
```
make package_ip && grep -A2 NEGO_CFG_RESET imp/fpga/tidelink_ip/component.xml
   # MODELPARAM_VALUE.NEGO_CFG_RESET must read bitString "1100001"
```

---

## 8. Merge-direction hazard — `fpga/scripts/merge_guard.sh`

`integ` has 6 × `fe_tx_credit_max_eff`; **`feat/dieb-clock-fix-wip` has 0.** A merge that resolves
`WlinkGenericFCSM_6.v` toward the die_b trunk **silently reverts the A→B fix that works on silicon**,
with every simulation still green. `merge_guard.sh` asserts `fe_tx_credit_max_eff`, `ext_lock_q` and
`EXT_STALL_LIMIT` survive any merge. Self-tested: passes on `wip/iter6-integ`, **fails on
`feat/dieb-clock-fix-wip`.** Run it after every merge, before every build.

`8eff9ce` (the fc_cfg APB arbiter) is on **no trunk** — not `integ`, not `dieb`, not `main`. The wedge
attribution was refuted, but the **APB protocol violation is real and independent**: an in-flight PS/AHB
access is preempted mid-ACCESS and never gets PREADY. On the ASIC the hang surface is
`cmsdk_ahb_to_apb` waiting on PREADY forever — no bootpy to blame, no bus timeout to save you.

---

## 9. Things I got wrong today (so you don't inherit them)

* `ws_kicked_q` disarm-park trap — mechanism real, **trigger unreachable**; refuted by an adversarial agent.
* `fc_cfg` preempt = the PS wedge — **refuted** (wrong shape: completed `0x018` DECERR, not a stall;
  wrong coverage). Keep the fix, drop the attribution.
* "EPOCH is the wrong lever" → then "EPOCH is the right lever" → **both wrong** (§6).
* "die_a commit-fails, die_b verify-fails" — an artifact of reading one terminal word (§6).
* "`0x21C0` is a free obs slot" — it is not; it falls through to the Wlink APB.
* "Add a `v1_elab` suite" — it already existed. The gate was never the gap; **CI was** (§1).
