# farm_gate.sh — sv_anti_pattern_lint accepted-baseline ratchet
# ==============================================================
# Each non-comment line is a KNOWN, accepted first-party sv_anti_pattern_lint
# finding present on the branch when the farm gate was introduced. The gate
# fails only on findings NOT listed here (i.e. a NEW latch / incomplete-case
# regression), so it is green today yet still catches new anti-patterns.
#
# Format: "<relpath-from-TIDELINK_HOME>:<line>: <CODE>" (message text dropped).
# To retire a baseline entry, fix the RTL and delete the line.
#
# Current accepted findings (refreshed 2026-07-11 on wip/tapeout-candidate).
# Every entry below was independently read-verified as a FALSE POSITIVE of the
# lint's top-of-block-default heuristic — no synthesizable latch is inferred:
#
#   * axi_chiplet_controller.sv ctrl_reg_rdata — APB read mux. `unique case
#     (ctrl_reg_addr[4:3])` enumerates all four 2-bit arms AND carries an
#     explicit `default: ctrl_reg_rdata = 32'b0;`. Every path assigns; flagged
#     only for lacking a pre-case default. (Was line 941; DRIFTED to 985 as the
#     region read-mux grew — same finding, new line.)
#   * tidelink_autoneg.sv selected_priority — the case is fully enumerated
#     (2-bit nego_pri_sel, all 4 arms 2'd0..2'd3 assigned) so no real latch is
#     inferred; the lint wants a defensive `default:` for tool-version safety.
#     (Was lines 382/383; DRIFTED to 541/542.)
#   * tidelink_lane_deskew_v2.sv ci / di (x3) — the flagged name in each case is
#     an `integer` FOR-LOOP ITERATOR (`for (ci|di = 0; ...)`), unconditionally
#     reset to 0 on every always_comb entry, so it can never latch. Each block
#     opens with explicit top-of-block defaults for its REAL outputs
#     (ep_changed=1'b0 @1202; ep_max_d/ep_min_d/ep_seen_active='0 @1235-1237;
#     max_dist/min_dist/sr_seen_active='0 @1374-1376) and writes the full
#     per-lane array every iteration. NO inferred latch. This V2-only file was
#     ABSENT on the branch that seeded the original baseline, hence its findings
#     were never listed; added here after line-by-line verification.
# These are pre-existing / verified-benign debt, tracked here so the gate
# ratchets forward.
src/rtl/local_overrides/axi_chiplet_controller.sv:987: COMB_NO_DEFAULT
src/rtl/local_overrides/tidelink_autoneg.sv:568: COMB_NO_DEFAULT
src/rtl/local_overrides/tidelink_autoneg.sv:569: CASE_NO_DEFAULT
src/rtl/local_overrides/tidelink_lane_deskew_v2.sv:1201: COMB_NO_DEFAULT
src/rtl/local_overrides/tidelink_lane_deskew_v2.sv:1234: COMB_NO_DEFAULT
src/rtl/local_overrides/tidelink_lane_deskew_v2.sv:1373: COMB_NO_DEFAULT
