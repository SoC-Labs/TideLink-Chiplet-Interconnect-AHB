# 2026-05-20 Session Summary

Major progress on TideLink autonomous I²C lock. **Autoneg now works on silicon.** Three RTL bugs identified, two fully fixed (silicon-validated), one fix applied with bench verification pending.

## What was working at start of day
- ✅ P15/P16 pinning (Arduino dedicated I²C, on-board pull-ups)
- ✅ BD Edit 1 (IOBUF + ila_i2c BD cell)
- ✅ All cocotb tests green on `feat/i2c-autonomous-lock-integ@e22528a`
- ❌ Silicon: master FSM stuck in CLAIM, bus inert. Diagnosis blocked.

## What's working at end of day
- ✅ Three bugs identified and committed (two silicon-verified)
- ✅ Autoneg arbitration completes on silicon
- ✅ Master role correctly assigned, both `role_locked=1`
- ✅ Full I²C transaction (PRESCALE/DATA/COMMAND AXIL + START + SCL clocking) captured via JTAG ILA
- ✅ `nego_done=1` on both boards
- ⚠️ Mask phase (states 8/9/10) still skipped — bug #3 candidate fix applied, bench verification pending

## Bug ledger

| # | Bug | Discovery | Fix | Silicon? |
|---|---|---|---|---|
| 1 | `role_lock=1` strap kills `nego_driving` (`role_in_nego = nego_en && !role_locked` is too strict) | Cocotb agent #1 | Sub `467b889`: gate on `nego_en` only | ✅ Verified |
| 2 | Synth-inferred latch on `txn_step_nxt` (`[Synth 8-327]`) | Cocotb agent #2 | Sub `be5eed2`: default `txn_step_nxt = txn_step_r` | ✅ Verified |
| 3 | Synth pruning of `mask_hs_auto_en` (NEGO_CFG[6]) — sim correct, silicon skips `if` branch | Bench ILA: `state_r==9` never triggers despite NEGO_CFG=0x61 readback | Sub `72bc582`: `(* keep *) wire mask_hs_auto_en_kept = mask_hs_auto_en;` + use in FSM | ⏳ Build #5 in progress (`bh7pvmvyk`) |

## Branch state at end of day
- Parent `feat/i2c-autonomous-lock-integ` @ **`3607c4f`** (pushed)
- Submodule `feat/i2c-autonomous-lock` @ **`72bc582`** (pushed)

## Cocotb regression (33 tests, all green)
- wlink_pair: 9/9 (link/assert bringup)
- wlink_pair test_autoneg_i2c_e2e: 3/3
- wlink_pair test_hw_repro_probe_seq (agent #1): 4/4
- wlink_pair test_hw_repro_full (agent #3): 3/3
- wlink_pair_full (agent #4 BD harness): 3/3
- tidelink_autoneg: 7/7
- i2c_mask_selflock: 3/3
- i2c_clkstretch: 1/1

## Cocotb agents launched this session
- Agent #1 (earlier session): Reproduced bug #1 in sim, applied fix
- Agent #2: Reproduced bug #1 + hunt for siblings → found bug #2 (latch)
- Agent #3: Force sim/HW parity → couldn't reproduce HW failure post bug #1+#2 fix (correct — they were necessary but not sufficient; bug #3 only shows on silicon)
- Agent #4: BD-IP wrapper harness + post-synth sim → both clean; confirmed sim/HW divergence is silicon-specific (synth/place&route layer)

## Bench actions this session
1. Built #4 diagnostic bitstream with mark_debug on 11 internal signals + `FPGA_INSERT_DEBUG_CORE=1`
2. Deployed, kicked autoneg, ran multi-capture ILA TCL
3. Confirmed: master drives bus, autoneg completes correctly (states 2→3→4→DONE)
4. **Key discovery**: state 9 (MASK_RD_ADDR) never visited on silicon — `hs_result=0` on slave proves master never wrote it via I²C
5. Released lease at end of day

## Next bench cycle (post build #5)

1. Stage new artefacts to mapstone (build artefacts will overwrite existing).
2. Deploy via vanilla `deploy_pair.sh`.
3. Kick autoneg via `run_i2c_test_fast.sh`.
4. Check `hs_result` on slave — should now be `0x01` if mask handshake ran.
5. Optionally arm ILA on `state_r==9` to confirm mask phase visited.

If bug #3 fix takes: full I²C autonomous lock with mask handshake operational. Then move to Phase 3 (link bring-up post-lock via `bringup_autocal_i2c.sh`).

If bug #3 fix doesn't take: try escalation paths in I2C_TODO Phase 2a (dont_touch, mark_debug, if→case restructure).
