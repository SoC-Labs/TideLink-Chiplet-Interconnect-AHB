// Coverage hierarchy file — restrict VCS coverage metrics to the DUT only.
// Excludes the UVM testbench, SVT VIP, and APB agent from code coverage.

+tree   test_top.u_dut          // Include the DUT hierarchy
-tree   test_top.ahb_fifo_if    // Exclude SVT AHB VIP interfaces
-tree   test_top.ahb_ret_if
-module test_top                // Exclude the testbench wrapper itself
